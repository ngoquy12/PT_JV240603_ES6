/** JSDoc
 * Hàm tính tổng hai số nguyên
 * @param {*} firstNumber Số nguyên thứ nhất
 * @param {*} secondNumber Số nguyên thứ hai
 * @returns Tổng hai số nguyên
 * Author: NVQUY (15/11/2024)
 * ModifiedAt: NVA(16/11/2025)
 */
export const sum = (firstNumber, secondNumber) => {
  return firstNumber + secondNumber;
};

const PI = Math.PI;

// Export default: Trong trường hợp file đó chỉ có 1 logic duy nhất
export const formatName = (firstName, lastName) => {
  return `${firstName} ${lastName}`;
};

export const formatName1 = (firstName, lastName) => {
  return `${firstName} ${lastName}`;
};

export const formatName2 = (firstName, lastName) => {
  return `${firstName} ${lastName}`;
};

export const formatName3 = (firstName, lastName) => {
  return `${firstName} ${lastName}`;
};

export const formatName4 = (firstName, lastName) => {
  return `${firstName} ${lastName}`;
};

export const formatName5 = (firstName, lastName) => {
  return `${firstName} ${lastName}`;
};

export { PI };
export default formatName;
